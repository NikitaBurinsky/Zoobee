# Zoobee 🐾

**Zoobee** — это комплексная платформа (маркетплейс/агрегатор) для поиска и продажи товаров для животных. Проект построен на базе **ASP.NET Core** с использованием принципов **Clean Architecture** (Чистой Архитектуры).

Система поддерживает различные роли пользователей (Покупатели, Организации, Администраторы), управление каталогом товаров, геолокацию зон доставки и автоматический парсинг товаров с внешних площадок (например, Zoobazar).

## 🚀 Технологический стек

* **Платформа:** .NET (C#)
* **Фреймворк:** ASP.NET Core MVC
* **ORM:** Entity Framework Core
* **База данных:** Postgres | EF Core
* **Архитектура:** N-Layer / Clean Architecture
* **Локализация:** Поддержка Русского (ru) и Белорусского (be) языков

## 📂 Структура проекта

Решение разделено на несколько логических слоев для обеспечения гибкости и тестируемости:

### 1. Zoobee.Core (Domain Layer)
Ядро приложения. Содержит основные сущности и логику, не зависящую от внешних библиотек.
* **DataEntities:** Основные модели (`Product`, `ZooStore`, `PetKind`, `Review`, `Brand`).
* **Identity:** Роли и пользователи (`AdminUser`, `CustomerUser`, `OrganisationUser`).
* **Enums & Primitives:** Типы данных, такие как `PetAgeRange`, `SizeDimensions`.
* **Localization:** Ресурсы для локализации ошибок и валидации.

### 2. Zoobee.Application (Service Layer)
Слой бизнес-логики.
* **DTOs:** Объекты передачи данных.
* **Interfaces:** Абстракции репозиториев, сервисов и UnitOfWork.
* **Services:** Реализация логики (например, `GeoLocationService`, `EnvirontmentDataSeedingService`).
* **Validation:** Валидаторы для DTO.

### 3. Zoobee.Infrastructure (Data Access Layer)
Реализация доступа к данным и внешним сервисам.
* **DbContext:** Конфигурация Entity Framework.
* **Repositories:** Реализация репозиториев для доступа к БД.
* **GeoServices:** Работа с геокодингом и проверка границ доставки (Polygon Geometry).
* **MediaStorage:** Сервисы для хранения файлов.

### 4. Zoobee.Infrastructure.Parsers
Отдельный модуль для скрапинга и обработки данных.
* **Pipelines:** Конвейеры обработки данных.
* **Parsers:** Реализация парсеров (например, для сайта *Zoobazar*).
* **Storage:** Сохранение спарсенных данных.

### 5. Zoobee.Web (Presentation Layer)
Веб-интерфейс приложения (папка `Zoobee.0.1`).
* **Controllers:** Контроллеры для разных областей (`Admin`, `Customer`, `Organisation`, `Sandbox`).
* **Views:** Razor Views (`.cshtml`).
* **Program.cs:** Точка входа и конфигурация DI контейнера.

### 6. Zoobee.Test
Модульные и интеграционные тесты для проверки бизнес-логики и сервисов.

## ✨ Ключевые возможности

* **Мульти-ролевая система:** Разграничение прав доступа для администраторов, магазинов (организаций) и покупателей.
* **Умный каталог:** Поддержка различных типов товаров (Еда, Туалеты/Наполнители) с специфическими атрибутами.
* **География и Доставка:**
    * Управление зонами доставки (`DeliveryArea`).
    * Проверка вхождения адреса в полигон доставки.
    * Интеграция с гео-сервисами.
* **Парсинг цен и товаров:** Автоматизированный сбор данных о товарах конкурентов или поставщиков.
* **Логирование:** Система ведет логи (файлы `Logs/log-YYYYMMDD.txt`).

## 🛠 Установка и запуск

1.  **Клонирование репозитория:**
    ```bash
    git clone [https://github.com/your-username/zoobee.git](https://github.com/your-username/zoobee.git)
    ```

2.  **Настройка базы данных:**
    Убедитесь, что в файле `appsettings.json` (в проекте `Zoobee.Web`) указана корректная строка подключения к базе данных.

3.  **Применение миграций:**
    Перейдите в директорию с решением и выполните обновление базы данных:
    ```bash
    dotnet ef database update --project Zoobee.Infrastructure --startup-project Zoobee.0.1
    ```
    *(Примечание: названия проектов могут отличаться, проверьте `.csproj` файлы)*

4.  **Запуск приложения:**
    ```bash
    cd Zoobee.0.1
    dotnet run
    ```

## 🧪 Тестирование

Проект содержит набор тестов, включая сидинг (seeding) тестовых данных (бренды, магазины, категории).
Для запуска тестов выполните:
```bash
dotnet test
